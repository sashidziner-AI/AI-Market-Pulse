node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
logs/
.playwright-mcp/
data/
*.db
outputs/
# Stray Playwright screenshots that occasionally land at the repo root when
# a test uses a `./name.png` path instead of `.playwright-mcp/`. Only matches
# top-level PNGs — public/*.png (logos, etc.) stays tracked.
/*.png
